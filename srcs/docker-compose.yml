services:
  nginx:
    build: ./nginx
    ports:
      - 443:443
    networks:
      - orchestra

  wordpress:
    build: ./wordpress
    networks:
      - orchestra

  mariadb:
    build: ./mariadb
    networks:
      - orchestra

# volumes:
#   db:
#
#   website:

networks:
  orchestra:
    name: "orchestra"


